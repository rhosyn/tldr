<main>
   <!--  <div class="container">
      <div class="start-text text-center">
          <h1>What do you want to see today?</h1>
        </div>
      </div> -->
  <div id="carousel">

    <!-- <div class="selected article" data-category="<%#= Article.find(4).newsapi_category %>">
      <p class="headline"><%#= "#{Article.find(4).newsapi_title}" %></p>
      <div class="card-image" style="background-image: url(<%#= image_path("#{Article.find(4).newsapi_imgeurl}") %>);">
      </div>

    </div> -->


    <% @articles.each do |a| %>
      <div class="hideRight article" data-category="<%= a.newsapi_category %>">
        <p class="headline"><%= "#{a.newsapi_title}" %></p>
        <div class="card-image" style="background-image: url(<%= image_path("#{a.newsapi_imgeurl}") %>);">
        </div>
      </div>
    <% end %>

  </div>

</main>
